fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
openai>=1.54.0
elevenlabs>=1.0.0
# Optional: required only for local audio capture
# pyaudio depends on the system portaudio library
# install manually with `pip install pyaudio` after
# installing portaudio headers (e.g. `apt-get install portaudio19-dev`)
#pyaudio>=0.2.11
numpy>=1.24.0
python-multipart>=0.0.6
sse-starlette>=1.6.5
python-dotenv>=1.0.0
pydantic>=2.5.0
aiofiles>=23.2.1
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
